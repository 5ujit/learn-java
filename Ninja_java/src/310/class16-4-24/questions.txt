create the list of integer value find find the size of list at the middle position insert the new element display



remove the element from the specified position








import java.util.*;

public class arrayadd {
    public static void main(String[] args) {

        // crete arraylist integer
        ArrayList<Integer> list = new ArrayList<Integer>();
        // user input
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the number of elements");
        int n = sc.nextInt();
        for (int i = 0; i < n; i++) {
            System.out.println("Enter the element");
            int ele = sc.nextInt();
            list.add(ele);
        }
        System.out.println(list);
        // find size of arraylist
        System.out.println(list.size());
        // find element in arraylist
        System.out.println(list.indexOf(10));
        // remove element from arraylist
        list.remove(0);
        System.out.println(list);
        // remove all element from arraylist
        list.clear();
        System.out.println(list);
        // sort arraylist
        Collections.sort(list);
        System.out.println(list);
        // reverse arraylist
        Collections.reverse(list);
        System.out.println(list);
        // sort arraylist
        Collections.shuffle(list);
        System.out.println(list);
        // sort arraylist
        Collections.swap(list, 0, 1);
        System.out.println(list);
        // sort arraylist
        Collections.swap(list, 1, 2);
        System.out.println(list);
        //  find first and last elemntof the list
        System.out.println(list.get(0));
        System.out.println(list.get(list.size() - 1));
        // find last element of the list
        System.out.println(list.lastIndexOf(10));
        // find last element of the list
        System.out.println(list.indexOf(10));
        // find last element of the list
        System.out.println(list.lastIndexOf(10));
        // find last element of the list
        System.out.println(list.indexOf(10));
        // find last element of the list
        System.out.println(list.lastIndexOf(10));
        // find last element of the list
        System.out.println(list.indexOf(10));
        // find last element of the list
        System.out.println(list.lastIndexOf(10));
        // find last element of the list



    }
}